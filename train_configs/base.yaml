experiment_name: base

train:
  shuffle: true
  batch_size: 16
  num_workers: 4
  epochs: 10
  lr: 1e-4

val:
  shuffle: false
  batch_size: 16
  num_workers: 4

data:
  labels_type: "phonemes"

model:
  input_size: 512
  hidden_size: 256